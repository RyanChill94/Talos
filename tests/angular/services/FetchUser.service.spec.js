ngDescribe({
    name: 'Test FetchUserService',
    modules: 'app',
    inject: 'FetchUserService',
    tests: function (deps) {

        it('basic test', () => {
            //
        });
    }
});
